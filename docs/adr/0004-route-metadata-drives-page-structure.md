# 以路由元数据驱动页面结构

工作台页面的标题、菜单归属、面包屑和标签页将由同一份路由注册表声明，`PageContainer` 与主布局从中读取，业务页面不再重复维护这些结构信息。顶栏只承载应用级操作，页面标题和面包屑由内容区 `PageContainer` 呈现。页面操作区保留为页面运行时注入的插槽，因为其可用性常取决于页面状态、权限和事件处理器；这种划分既保持导航结构的一致性，又不让全局元数据承担业务行为。
